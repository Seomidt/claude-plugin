{
  "name": "claude-plugin",
  "version": "1.0.0",
  "description": "Plugin system for Claude Code — install, manage, and run plugins from registries",
  "main": "src/index.js",
  "bin": {
    "claude-plugin": "bin/claude-plugin.js"
  },
  "scripts": {
    "test": "node --test test/plugin-manager.test.js",
    "lint": "node --check src/ bin/"
  },
  "keywords": ["claude", "plugin", "cli", "anthropic"],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
